# ============== BACKEND (API) ==============
NODE_ENV=development
PORT=3000
CORS_ORIGIN=http://localhost:4200
DATABASE_URL=postgresql://dbuser:dbpass@localhost:5432/inventory

# ============== FRONTEND (WEB) ==============
API_BASE_URL=http://localhost:3000

# ============== PRODUCTION DEPLOYMENT ==============
# Backend (Vercel): Set DATABASE_URL via Vercel dashboard from Render
# Frontend (Netlify): Set API_BASE_URL=https://inventory-api.vercel.app via Netlify UI
